[www]
user = www-data
group = www-data
listen = 9001
pm = dynamic
pm.max_children = 5
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3
clear_env = no

; Set the PHP-FPM master process timeout to 10 minutes
request_terminate_timeout = 600